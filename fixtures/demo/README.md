# Demonstration fixtures

EAT-003 has prepared `selection-manifest.v1.json` with a proposed public pair, exact object identities, access evidence, checksums, comparability, interpretation limits, and sensitivity controls. Its status remains `awaiting_owner_approval`; it does not authorize imagery download or public use.

Small, licensed, reproducible derived demonstration artifacts will be introduced only after the selection is approved and EAT-007 defines the bundle contract.

Raw SAR imagery and large generated artifacts must not be committed here.
