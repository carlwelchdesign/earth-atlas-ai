# EAT-031 Nepal investigation brief evidence

Verified locally on 7 September 2026.

- JSON and printable HTML are derived from the same versioned brief record without a model call.
- Both formats contain the case version, question, exact acquisition IDs and UTC timestamps, source observations, current user assessment, correction history, unresolved observation IDs, citations, quality limitations, and attribution.
- Printable HTML resolves the prepared before/after thumbnails from the deployment origin.
- Browser verification downloaded both files. The JSON was parsed and reconciled; the downloaded HTML was reopened as a local file and rendered with both imagery panels.
- Tests cover deterministic output, matching references and dates, correction history, unresolved observations, and HTML escaping of hostile user text.

Artifact: `images/eat-031-nepal-brief.png`.
